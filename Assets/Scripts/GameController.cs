using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GameController : MonoBehaviour
{
    //[SerializeField] Camera playerCamera;
    //[SerializeField] float playerCamRotX;
    //[SerializeField] float playerCamRotY;
    //[SerializeField] float playerCamRotZ;

    // Start is called before the first frame update
    void Start()
    {
    //    playerCamera.transform.position = new Vector3(1,0,-1);
    }

    // Update is called once per frame
    void Update()
    {
      //  playerCamera.transform.Rotate(playerCamRotX, playerCamRotY, playerCamRotZ);
        
    }
}
